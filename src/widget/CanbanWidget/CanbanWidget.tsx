export const CanbanWidget = () => {
  const classNames = {
    container: 'flex flex-col gap-[50px]',
    buttonBlock: 'flex gap-[20px]',
  };

  return <div className={classNames.container}></div>;
};
